# Configuración de urlwatch

display:
  unchanged: false
  error: true
  
report:
  text:
    details: true
    footer: true
    
  telegram:
    enabled: true
    bot_token: ${TELEGRAM_BOT_TOKEN}
    chat_id: ${TELEGRAM_CHAT_ID}

job_defaults:
  diff_tool: unified
  compared_versions: 2